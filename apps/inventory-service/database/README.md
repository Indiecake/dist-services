# inventory-service database

This service owns `inventory_schema`. Structural SQL is generated by drizzle-kit into `database/migrations/`.

Local catalog data is not part of migrations. After Postgres is up, seed `sku-1` and `sku-2` (with names, prices, and a General category) with:

```bash
pnpm db:seed
```
